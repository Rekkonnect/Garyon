name: run-tests-windows

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  run:
    runs-on: windows-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v6

    - name: Run tests
      run: dotnet test --project Garyon.Tests/Garyon.Tests.csproj